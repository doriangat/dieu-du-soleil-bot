const Discord = require('discord.js');
const fs = require('fs');

module.exports = {
	name: 'help2',
	description: "Envoie le lien avec quelques commandes",
}

module.exports.run = async (bot, message, args) => {

    {
{
    
        var couteau = new Discord.Message()
        message.reply("Regarde tes mp bro")
        message.author.send("Fait pas le con avec ces commandes: \n***https://urlz.fr/bC5U***")
        } 
    }
}

module.exports.help = {
    name: "help2"
}