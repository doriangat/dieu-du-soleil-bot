const Discord = require('discord.js');
const fs = require('fs');

module.exports = {
	name: 'p',
	description: "j'utilise pour faire parler le bot alors touche pas",
}

module.exports.run = async (bot, message, args) => {

    {
{
        message.delete();
        var couteau = new Discord.Message()
        message.channel.send("venez svp")
        } 
    }
}

module.exports.help = {
    name: "p"
}