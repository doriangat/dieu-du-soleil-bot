const Discord = require('discord.js');
const fs = require('fs');

module.exports = {
	name: 'h',
	description: "Insulte les h",
}

module.exports.run = async (bot, message, args) => {

    {
{
        message.delete();
        var couteau = new Discord.Message()
        message.channel.send("Fuck les H")
        } 
    }
}

module.exports.help = {
    name: "h"
}