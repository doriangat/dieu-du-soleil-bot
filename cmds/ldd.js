const Discord = require('discord.js');
const fs = require('fs');

module.exports = {
	name: 'ldd',
	description: "Dit que LowDeeDa c'est juste le meilleur",
}

module.exports.run = async (bot, message, args) => {

    {
{
        message.delete();
        var couteau = new Discord.Message()
        message.channel.send("LowDeeDa c'est juste le meilleur enfaite")
        } 
    }
}

module.exports.help = {
    name: "ldd"
}